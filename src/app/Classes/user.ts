import { Area } from "./Area";

export class User {
  userId?:number ;
  password: string;
  name?:string
  emailId: string;
  phoneNumber: string;
  role?: string; 
  selectedAreas?:any[]
  status?: string;
 
}
