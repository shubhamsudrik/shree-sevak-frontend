import { BaithakSearchPipe } from './baithak-search.pipe';

describe('BaithakSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BaithakSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
