import { ScheduledFilterPipe } from './scheduled-filter.pipe';

describe('ScheduledFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduledFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
